WINDOW_WIDTH = love.graphics.getWidth();
WINDOW_HEIGHT = love.graphics.getHeight();

-- Player constants
MOVEMENT_SPEED = 0.3;
STARTING_POSITION = 30;
PLAYER_HEIGHT = 50;
PLAYER_WIDTH = 30;

-- Background constants
BACKGROUND_HEIGHT = 100;

-- resets color to be white
function resetColor()
    love.graphics.setColor(1, 1, 1);
end